import React from 'react';
import { View } from '@tarojs/components';
import CustomTabbar from '../../components/CustomTabbar';

const Calendar = () => (
  <>
    <View style={{ paddingBottom: '60px' }}>
      <View>日历页面</View>
    </View>
    <CustomTabbar />
  </>
);

export default Calendar;
